package org.pingpong.onequarkusapp;

import io.quarkus.test.junit.NativeImageTest;

@NativeImageTest
public class NativeResourceIT extends ResourcesTest {

    // Execute the same tests but in native mode.
}